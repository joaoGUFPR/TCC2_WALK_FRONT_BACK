ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ br.com.ApplicationKt.module ]
    }
}
postgres {
  url = "jdbc:postgresql://localhost/default"
  user = "username"
  password = "password"
}